# 壁打ちログ — SCENARIO_SKELETON.md（2026-08-09）

## 判定: **NO-GO（1往復目終了時点）**

1往復目で CRITICAL が7件確定。2往復目へ続行。

対象: `docs/SCENARIO_SKELETON.md`（＋実装 js/*.js・tools/smoke_test.js）
方式: 4視点を毎往復まっさらなサブエージェントで並列起動（`/kabeuchi` の流儀）

**このログの方針**：レビュアーの指摘は、ファイル・行番号・実測値で**こちらが裏を取ったものだけ**を採用として記録する。
裏が取れなかったもの・実物と食い違ったものは「不採用」に理由つきで残す。

---

## 1往復目

### メタ読みプレイヤー視点

| 重大度 | 指摘 | 反映 |
|---|---|---|
| CRITICAL | 「……」を含む＝正解88%、「……」で始まる＝94%、中に引用符＝95%。文字数テルを潰した先に、より精度の高いテルが残存 | **反映**＝§5 を全面差し替え。あわせて `smoke_test.js` の検出器を「1変数ルール18種の的中率」へ作り直し（決め打ちをやめた） |
| CRITICAL | EP11以降が完全なワンパターン（desc小道具→見抜く→過去→小さな決断）。院長は「人前で何を喋るか迷う」4回、芸能社長は「捨てられない古い物」8回 | **反映**＝傷 P |
| HIGH | VIP3人の反応文法が同一。「────」院長40／御曹司94／芸能社長107。「たまに怖いこと言うね」が院長と芸能社長で逐語一致 | **反映**＝傷 L（「────」の数は自前で再計測） |

### 攻略勢視点

| 重大度 | 指摘 | 反映 |
|---|---|---|
| CRITICAL | 「……」を押す／おねだりは上から2番目／雑用は全部断る、の3手で最短 Day37 | **一部反映**。テルは §5、断りの exploit は**実装を修正済み**（下記）。Day37 は自前の計測では再現せず（最適プレイ Day53〜62）＝**未確認**として保留 |
| CRITICAL | `missionDecline` が `missionIdx` を進める＝断ると話を1本消して営業日も失わずに先へ進める | **反映＋実装修正済**。断っても消えず後日また来る／二度断られたら来ない（`CONFIG.mission.dropAfter`） |
| CRITICAL | `buildOffer` が「確実に通る1本」を必ず提示するため、値段から内部状態が復元できる（上から2番目が成功率99.3%） | **未反映**。game.js:1316-1326 の構造は確認したが、成功率は自前で再現していない＝**2往復目へ持ち越し** |
| HIGH | 自己投資が全部マイナスEV（整形フルで+17.4日・筋トレで+16.0日）＝最適ビルドが「何も育てない」 | **未反映**。育成SIMとして致命的だが、数値の再現をしていない＝**2往復目へ持ち越し** |
| — | 「受諾と同じ関係値が入る」 | **不採用**。実装は `declineAff: -15` で好感度は下がる。壊れていたのは値ではなく「話が消える」点 |

### 実装整合視点

| 重大度 | 指摘 | 反映 |
|---|---|---|
| CRITICAL | 勝ちプレイでは VIP の大半が到達不能（実測 院長10〜28／御曹司4〜17／芸能社長4〜16） | **反映**＝§2 に自前の再計測（最適 11/5/5・中級 23/15/13）を掲載。**作者決裁で方針5項目を確定** |
| CRITICAL | 場内ユニットが 7/6/6 本しかなく、上手いプレイほど先に枯れて逐語再演される。§6「場内は約半分」は上手いプレイでは逆（86〜88%） | **反映**＝§6 の在庫行を探り／場内に分割、システム前提2を訂正 |
| CRITICAL | §5 の選択肢設計が4行とも実装と不一致（正解 +15〜20 → 実装 +7〜+12／地雷の心コストが空欄 → 実装 −8で最大） | **反映**＝§5 の表を実装値へ全面差し替え |
| CRITICAL | §5 に「〈険〉ではハズレも即爆発」（game.js:1039）が書かれていない | **反映**＝§5 に「書かれていなかったルール2つ」を追記 |
| HIGH | 「533卓」が誤り。実測 554卓（骨格内でも 143+132+135+144=554 と自己矛盾） | **反映**＝3箇所とも訂正 |
| HIGH | README.md:3 が「設計の正は GAME_DESIGN.md」と宣言しており、本ファイルと三つ巴で矛盾 | **反映**＝傷 B を 中→高。冒頭に README の乖離を明記 |
| HIGH | 死にフラグ 22/60（37%）。`shitencho_teinen`・`sekiyu_kikoku` は §2 が「期限つき」の根拠にしている当のフラグだが、`custActive` に退場日の概念が無く Day99 でも支店長は来る | **反映**＝傷 R |
| HIGH | `smoke_test.js` はレッドかつフレーキー（`geino_f_sofa` 未到達が8回中3回）。throw が途中にあり後続の出力が出ない | **一部実装済**＝throw を末尾へ移し `FAILURES` に集約。coverage の緩和は**未着手** |
| MEDIUM | 新テル「数字を含む＝正解64%」 | **反映**＝§5 の表に追加（自前の検出器でも同値） |
| MEDIUM | 死にコード `epIdx`／`donperiCount`／`tierFloor`／`helpTrouble.maxPerNight` | **反映**＝傷 T |
| MEDIUM | `enterJonai` の `\|\| all[0]` が needs も bindFirst も検証しない | **反映**＝§6 システム前提4。**§2 の並べ替えを行う際の最大のリスク**として明記 |
| MEDIUM | 御曹司の年齢破綻（EP9で27→EP17で30→EP35で28） | **反映**＝傷 S |
| — | `noSundayMeet` はどの客にも定義されていない死にフラグ | **不採用（誤り）**。石油王 customers.js:4243 に定義あり。ご隠居・会長に無い点のみ傷 Q として採用 |
| — | §4 の「エピローグ（ツアー初日）が無い」は既に実装済み | **反映**＝§4 を訂正（下記「不採用」も参照） |

### 演出・キャラ濃淡視点

| 重大度 | 指摘 | 反映 |
|---|---|---|
| CRITICAL | VIP の底は EP7〜11 で終わり、以降117話は全部3ラリー | **反映**＝§2（実装整合の指摘と同一の穴。両視点が独立に到達） |
| CRITICAL | 「2月1日＝息子の試験日。空けておけ」「カツサンド」が、customers.js 全体でその1卓にしか出てこない | **反映**＝傷 I（grep で確認） |
| CRITICAL | 出口の約束（院長EP11・芸能社長EP10）がエンディングで回収されない。`ending.win` に客が一人も出てこない | **反映**＝傷 J・§4。§2 の方針2（条件で撃つ）で同時に解く |
| CRITICAL | 主人公が「見抜き装置」に固定。芸能社長EP1「たまには間違えな。人間はミスで好きになられんの」＝最良の仕込みが100日回収されない | **反映**＝傷 A を補強 |
| CRITICAL | VIP3人の底が同型（全員「父的存在からの一言を20〜40年抱えている男」）＋石油王EP2でダメ押し | **反映**＝傷 K。§1 の差別化表が台詞レベルで守られていない |
| HIGH | 推しに接続している客が芸能社長のみ（9／課長1／石油王1／他9人0）。御曹司は主人公の目標を知らない | **反映**＝傷 M（自前で再計測） |
| HIGH | レイナが Day3 で消滅。100日間、主人公の成長を目撃する外部視点がゼロ | **反映**＝傷 N |
| HIGH | 母が「詫びるだけの人」。Day55 は選択肢が両方とも敗北、Day90 は主人公が何もしていないのに母が折れる | **反映**＝傷 O |
| — | エピローグ（ツアー初日）が存在しない | **不採用（誤り）**。`ending.win` にツアー初日まで書かれている。穴は「無い」ではなく「2文しかない／客が出てこない」→ 傷 J に置き換え |
| — | 御曹司EP27「三十過ぎた弟が、泣いたのよ」＝矛盾 | **不採用（誤り）**。実物は「弟がさ、就職決まったって」だけで年齢に触れていない |

---

## 1往復目で実装まで進めたもの（本来は別セッションの領分・作者指示により先行）

- `tools/smoke_test.js` のテル検出器を作り直し（1変数ルール18種・45%基準・判定は末尾へ集約）
- `ishi` 第3話1卓目の正解欠落を修正（554卓でここだけ正解も凡打も無かった）
- `missionDecline` の話消し exploit を修正（`CONFIG.mission.dropAfter` を追加）
- 「満場一致で通った」の逐語重複を解消（御曹司側を彼の傷＝実力か名字か に書き直し）

コミット: `54416cd`

---

## 未実施

- 2往復目・3往復目
- Phase 4 S級採点ゲート（新規エージェント・7軸）
- 攻略勢の持ち越し2件（`buildOffer` の内部状態漏れ／自己投資のマイナスEV）の再現検証
